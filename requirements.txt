# MolManager — full Python dependency list (core + ML + docking + dev).
# For a smaller desktop-only install, prefer requirements-core.txt instead.
# From the repo root with your virtual environment active:
#   pip install -U pip
#   pip install -r requirements.txt
#   pip install -e .
#
# Includes the desktop app, pKa predictor (PyTorch + unipkainfer), permeability (Chemprop),
# docking helpers (Meeko, PDBFixer), and development tools (pytest).

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Desktop app and chemistry core ---
PyQt5>=5.15
# Embedded 3D structure viewer (View in 3D); without it, the app opens the browser instead.
PyQtWebEngine>=5.15
# Keep Plotly in the v5 line for broader Qt WebEngine compatibility.
plotly>=5.0,<6
# RDKit wheels are built against NumPy 1.x; NumPy 2.x causes _ARRAY_API / import failures.
numpy>=1.24,<2
pandas>=2.0
scipy>=1.10
sqlalchemy>=2.0
pubchempy>=1.0.4
chembl_webresource_client>=0.10.9
rdkit>=2022.9.5
# Fast JSON for compact .cms session files.
orjson>=3.9
# Official Open Babel wheels (obabel + Confab) for Systematic conformers.
openbabel>=3.2.0
scikit-learn>=1.3
umap-learn>=0.5.5

# --- PyTorch (CPU 2.5.1; pKa and permeability tools) ---
torch==2.5.1
torchvision==0.20.1

# --- pKa predictor (Tools → Predict → pKa; Uni-pKa / unipkainfer) ---
unipkainfer>=0.2.0

# --- Permeability prediction (Tools → Predict Permeability / GNN-MTL) ---
chemprop>=2.1.0,<2.3
lightning>=2.0,<3

# --- Docking (Tools → Dock → Dock / Prepare / Prepare PDB / Smina CLI) ---
meeko>=0.5.0
pdbfixer>=1.9
# OpenMM 8.3+ can segfault during PDBFixer hydrogen placement on Windows (use 8.2.x).
openmm>=8.2,<8.3
easydock>=1.3.1

# --- Development / CI ---
pytest>=7.4,<9
ruff>=0.6
pip-audit>=2.7
