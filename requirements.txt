# mctoolkit — full Python dependency list (core + ML + docking + dev).
# For a smaller desktop-only install, prefer requirements-core.txt instead.
# From the repo root with your virtual environment active:
#   pip install -U pip
#   pip install -r requirements.txt
#   pip install -e .
#
# Includes the desktop app, pKa predictor (PyTorch + unipkainfer), permeability (Chemprop),
# docking helpers (Meeko, PDBFixer), and development tools (pytest).

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Desktop app and chemistry core ---
# Qt WebEngine (embedded 3D / Plotly) ships with the PySide6 wheels.
# Qt 6.8 LTS. 6.11+ wheels fail to import on Windows 10 (System32 ICU 64 vs Qt's ICU 78).
PySide6>=6.8,<6.9
# Keep Plotly in the v5 line for broader Qt WebEngine compatibility.
plotly>=5.0,<6
# Stay on NumPy 1.x: PyTorch 2.5.1 and umap-learn/numba are still more reliable on
# 1.26 than on NumPy 2. RDKit 2025.9+ supports either; this pin is not an RDKit ABI limit.
numpy>=1.24,<2
pandas>=2.0
openpyxl>=3.1
scipy>=1.10
sqlalchemy>=2.0
pubchempy>=1.0.4
chembl_webresource_client>=0.10.9
# Official PyPI name is `rdkit`. Do not install `rdkit-pypi` (last release 2022.9.5);
# both packages share the `rdkit` import path and the old one will shadow the new wheels.
rdkit>=2025.9.1
# Fast JSON for compact .mct session files.
orjson>=3.9
# Official Open Babel wheels (obabel + Confab) for Systematic conformers.
openbabel>=3.2.0
scikit-learn>=1.3
umap-learn>=0.5.5

# --- PyTorch (CPU 2.5.1; pKa and permeability tools) ---
torch==2.5.1
torchvision==0.20.1

# --- pKa predictor (Tools → Predict → pKa; Uni-pKa / unipkainfer) ---
unipkainfer>=0.2.0
# Fold weights (~550 MB) are not a pip artifact. After this file:
#   python scripts/bootstrap_unipka_model.py
# or run scripts/install_pytorch_pka.ps1 / .sh (also selects CUDA when nvidia-smi sees a GPU).

# --- Permeability prediction (Tools → Predict Permeability / GNN-MTL) ---
chemprop>=2.1.0,<2.3
lightning>=2.0,<3

# --- Docking (Protein → Dock Ligand → Prepare / Gnina) ---
meeko>=0.5.0
# Meeko does not always install gemmi; receptor PDBQT import needs it.
gemmi>=0.6
pdbfixer>=1.9
# OpenMM 8.3+ can segfault during PDBFixer hydrogen placement on Windows (use 8.2.x).
openmm>=8.2,<8.3
pdb2pqr>=3.6.1
# Protein Viewer interaction overlays (H-bond / hydrophobic / ionic / π / halogen).
prolif>=2.1

# --- Development / CI ---
pytest>=7.4,<9
ruff>=0.6
pip-audit>=2.7
